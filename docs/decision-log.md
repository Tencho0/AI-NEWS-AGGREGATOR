# Decision Log

Append-only index of every decision. Full rationale for numbered entries lives in `adr/`.
Lightweight decisions (no ADR needed) are recorded here directly.

| Date | Ref | Decision | Status |
|---|---|---|---|
| 2026-07-02 | ADR-0001 | Record decisions as ADRs | Accepted |
| 2026-07-02 | ADR-0002 | .NET 10 worker service platform | Proposed |
| 2026-07-02 | ADR-0003 | Separate `Newsroom` DB on existing SQL Express; Dapper | Proposed |
| 2026-07-02 | ADR-0004 | In-process hosted-service scheduling for v1 | Proposed |
| 2026-07-02 | ADR-0005 | Anthropic Claude, model-per-stage (Opus 4.8 drafting / Haiku 4.5 analysis), Batch + caching + cost cap | Rejected → ADR-0010 |
| 2026-07-02 | ADR-0006 | Telegram long-polling bot as review surface | Proposed |
| 2026-07-02 | ADR-0007 | Dedicated publishing endpoint inside the Umbraco site | Proposed |
| 2026-07-02 | ADR-0008 | Facebook Page automation only; groups stay manual (API deprecated) | Proposed |
| 2026-07-02 | ADR-0009 | Image sourcing chain; never reuse scraped images | Proposed |
| 2026-07-02 | — | Content language: Bulgarian only (matches site) | Noted (assumption A-2) |
| 2026-07-02 | — | Human approval mandatory for every publish in v1 (no full-auto mode) | Noted (scope) |
| 2026-07-02 | ADR-0010 | Provider-agnostic AI layer via `Microsoft.Extensions.AI.IChatClient`; **Gemini free tier as default provider** (owner decision); quota budgets replace dollar caps as the primary limit | Accepted |
| 2026-07-02 | research/2026-07-free-ai-providers.md | Free-provider survey: shortlist Groq (analysis overflow) + Mistral Experiment (drafting candidate) + BgGPT/INSAIT (Bulgarian-first); GitHub Models dev-only; skip Cerebras/OpenRouter | Noted |
| 2026-07-02 | — | **Owner: stay with Gemini API only at launch** — research shortlist parked; no secondary providers wired; provider abstraction (ADR-0010) retained as the escape hatch | Accepted |
| 2026-07-02 | — | Gemini API key provisioned; stored via `dotnet user-secrets` (dev) / env var (VPS) — never in the repo; rotate when convenient (key transited chat) | Noted |
| 2026-07-03 | — | Trend v1 simplifications: one topic per article (unique link, no similarity scores); no Hot demotion; trend scoring is pure code (AI only assists clustering) — formula documented in 02-functional-spec.md and `TrendScorer` | Noted |
| 2026-07-03 | — | Q-2 style guide drafted from the owner's sample articles (`docs/editorial-style-guide.md`, PROMPT-START/END block is the prompt source, single-sourced into Core as an embedded resource); Phase 3 proceeds on the draft, owner review pending | Proposed |
| 2026-07-03 | — | Publishing contract v1 (owner may veto): automated articles use a dedicated **"Predel News" staff author** (resolves Q-6); `coverImage` falls back to a **configured placeholder media item** when the draft has no image (stock keys pending); tags are find-or-create by name; category/region resolved by node name | Proposed |
| 2026-07-03 | — | Facebook v1 semantics: DryRun default ON (staging mode; completes flow as "(пробен режим)" success); FB failures never block/roll back the site publish — draft sits `PartiallyPublished` with retries until the page post lands; OAuth errors terminal + daily token health probe | Noted |
| 2026-07-03 | — | **Q-1 sources onboarded** (owner list): БТА `/bg/rss/free`, Mediapool `/rss`, Струма `/feed`, Топ Преса `/feed`, ИнфоМрежа `/rss`, Благоевград24 `/rss/` — feeds verified with the bot UA 2026-07-03; robots.txt enforced at runtime by the scraper. Deferred: pirinsko.com (no RSS), blagoevgrad.bg (sitemap only) → backlog sitemap/html adapters. 48 h M1 collection run started 2026-07-03 ~21:47 (detached worker `C:\apps\newsroom-dev`, publishing leg dormant) | Accepted |
| 2026-07-08 | — | **Publishing target for now: Facebook only — website (Umbraco) leg paused** (owner decision). The Predel site needs polish before it receives automated articles, so Approved drafts publish only to the FB Page (plain text, no link) and the Umbraco leg is skipped entirely (no site call, no fake publish record). Temporary operational override of the site-first flow in ADR-0007/0008 — revisit once the site is ready. **Implemented** as a reversible toggle `Publishing:FacebookOnly` (default off): on = skip Umbraco, post Approved drafts straight to FB (Approved → Published via new `IPublishRepository.GetApprovedForFacebookAsync`); off = full site → Facebook pipeline unchanged. **To re-enable the website:** set `Publishing:FacebookOnly=false` and configure `Umbraco:BaseUrl`/`ClientSecret` — no code change. **To go live now:** `Publishing:FacebookOnly=true` + `Facebook:DryRun=false` + restart. Drafts published while FB-only was on keep a `facebook`-only ledger and are NOT back-filled to the site later (owner choice). | Accepted |
| 2026-07-13 | — | Editor-authored articles via Telegram: /post (verbatim, no AI) and /new (AI from editor notes); synthetic Manual topics reuse the force-draft pipeline | Accepted |
| 2026-07-17 | superpowers/specs/2026-07-17-facebook-engagement-design.md | **FB engagement round** (low reach diagnosis): social-native FB caption (sentence-case hook + CTA + 2–3 hashtags) generated in the same drafting call — deviates from house style **on FB only**, article keeps the style guide; suggested best publish time + 📅 Насрочи button on the review card (✅ Одобри stays instant); insights polling deferred to backlog ("smart insights") | Accepted |
| 2026-07-17 | superpowers/specs/2026-07-17-promotion-strategy-design.md | **Promotion strategy** (reach the largest audience): region-first, organic-only; search/Discover as the owned backbone + FB/Instagram amplifiers + reader-tips moat; new channel = Instagram; short video (TikTok/Shorts/Reels) **deferred 2026-07-18**; Viber/Telegram channel declined; ~1 h/day editor time | Accepted |
| 2026-07-18 | — | **Production domain is `predelnews.com`** (not `.bg`): the planning docs and acquired domain are authoritative; the `predelnews.bg` strings hardcoded in the Predel-News site (`robots.txt`, `_JsonLd.cshtml`) were bugs, fixed in the SEO/Discover pack. All canonical/JSON-LD/sitemap/robots URLs and the GSC property use `predelnews.com` | Accepted |
| 2026-07-18 | — | **Stored article dates are treated as UTC** and emitted as `...Z` (ISO-8601 UTC) in JSON-LD, OG `article:*_time`, and the news sitemap. Worker-published `publishDate` and `updatedDate` are already UTC; Umbraco returns them `Kind=Unspecified`, so the code specifies UTC rather than appending the server-local offset (the prior `zzz` formatting was wrong by 2–3 h). Editor-entered local times in the backoffice are accepted as a known small skew (documented in `runbooks/google-search-console.md`); `updatedDate` of `DateTime.MinValue` is treated as "never modified" so `dateModified` stays absent | Accepted |
| 2026-07-18 | superpowers/plans/2026-07-18-seo-discover-pack.md (Predel-News repo) | **SEO/Discover pack implemented** in Predel-News: `/news-sitemap.xml` (48 h window, current Google News schema — publication/date/title only), NewsArticle JSON-LD rebuilt via a Core builder (absolute ≥1200 px image set, UTC dates, org-vs-person author), `max-image-preview:large` robots meta (search page `noindex,follow`), absolute 1200×630 og:image + og:image:alt + article:section/tag, config-driven `google-site-verification` tag, sub-1200 px cover-image warning in the publishing endpoint, and a fix for a pre-existing `/sitemap.xml` 500 (missing UmbracoContext). Publisher Center submission is obsolete (removed 2024/25 — News/Discover inclusion is automatic); GSC steps in `runbooks/google-search-console.md` | Accepted |
| 2026-07-30 | ADR-0011 | **Cloudflare Workers AI (FLUX.1 Schnell) generates the cover image** from the article's own details before Telegram review (resolves Q-4); stock providers (Pexels/Pixabay) become the automatic fallback on any failure or exhausted quota; `Ai:Stages:Image` budget caps the free tier; credentials in user-secrets / service env vars | Accepted (style/model superseded by ADR-0012) |
| 2026-07-31 | ADR-0013 | **Cover text is burnt in by the model; the logo never is.** The drafting model returns `coverHeadline` (≤42 chars BG), `coverKeyPoints` (0–3, ≤18 chars), `coverTextEmphasis` (headline\|number) and `coverTextPlacement`; the prompt passes them **verbatim in quotes** with explicit placement/hierarchy/typography/alignment/colour/contrast and fences out all other text — the real Predel News logo is composited from its own PNG after generation (`Images:Cover:*`), so the brand mark is always exact. Telegram review becomes load-bearing: every rendered Cyrillic glyph must be eyeballed before publish. Prompt assembly now budgets the scene instead of tail-truncating, so the no-text/no-logo fences can never be cut off. **Quota classification fixed:** only Cloudflare code 3036 (or HTTP 402) arms the until-midnight lock; code 3040 is transient (bounded retry, then stock for that draft only) and the message-keyword heuristic is deleted — a capacity blip no longer costs a day of covers. **Oversized reference portraits are resized** (aspect-preserving, long side 511, never upscaled) instead of dropped. **Images move out of the deployment directory:** persistent `Images:StorageRoot` (mounted volume in production) with generated-images/editor-uploads/public-figures areas; `nw_DraftImage.Url` stores a relative storage key resolved through the root, legacy absolute paths still resolve, and traversal is refused by construction. **Daily retention pass** (migration 0014 adds `FilePrunedAtUtc`): 14 d for generated covers on discarded drafts and unselected covers on published ones, 30 d for editor uploads and for published covers (Umbraco is durable); reference photos and anything on an active draft are never touched. New dependency: SkiaSharp (MIT, native) for the two pixel operations | Accepted |
| 2026-07-31 | ADR-0012 | **Covers become cinematic photoreal news artwork, not illustration** — model switched to `@cf/black-forest-labs/flux-2-klein-4b` (multipart wire format, neuron-priced inside the free daily allocation, ≈92 neurons/cover); the drafting model now returns one coherent English `imageScene` (subject, place, time of day, weather) instead of the prompt joining stock keywords; category demoted to a mood/colour tint; 16:9 with central safe area + calm lower third and upper-right corner reserved for later headline/number/branding layers (**overlay renderer not built yet**). **Public figures** may be depicted only from an approved reference photo in `Images:PublicFigures` — the drafting model judges centrality (`imageCentralPerson`), and four gates (configured name, non-sensitive category, readable <512 px reference, multipart format) must all pass; crime/scandal stories default to a symbolic scene. Attribution stays "Илюстрация" (owner). **Never paid:** 429/402/spend-wall wording stops generation until UTC midnight, alerts the editor on Telegram and falls back to stock — no retry, no escalation to a billable model | Accepted |
| 2026-08-03 | — | **Rollback: AI cover generation (Cloudflare Workers AI FLUX, ADR-0011/0012/0013) disabled; covers revert to free stock APIs (Pexels/Pixabay, ADR-0009 tier 2) only.** Implemented as a config kill switch, not a code removal: `CloudflareImagesOptions.Enabled` now gates `IsConfigured` and defaults to `false` when bound from configuration (`Images:Cloudflare:Enabled`), independent of whether `AccountId`/`ApiToken` are set — so a deployment with old credentials still in user-secrets does not silently resume generating. All FLUX/cover-text/public-figure/logo code stays in the repo, tested, one flag away from re-enabling. **To re-enable:** `Images:Cloudflare:Enabled=true` + valid `AccountId`/`ApiToken` | Accepted |
| 2026-08-04 | ADR-0014 | **Sandbox mode adopted:** a second `Sandbox` hosting environment runs side by side with live, isolated by its own user-secrets store (`newsroom-worker-sandbox`, never loaded by `Development`), a fail-closed startup guard (refuses to start unless the database name ends `_Sandbox`, the site is `localhost`/`127.0.0.1`, and the image root contains `sandbox`), and forced-in-code `Facebook:DryRun=true` / `Publishing:FacebookOnly=false` overrides. This dev machine's own `Development`-environment worker (used before the sandbox existed) was relocated from `bin\Debug` to its own local `C:\apps\newsroom`, so a `dotnet build` no longer needs it stopped first. **Topology correction, made after an incident during verification:** the live pipeline runs as the Windows Service `PredelNewsroom` on the Predel-News VPS, not on this dev machine — a stray `dotnet run` here loaded this machine's `Development` secrets (which still hold the real Telegram bot token) and collided with the VPS's own poller (`409 Conflict: terminated by other getUpdates request`), sending one duplicate live daily digest before being killed. This dev machine now runs the **sandbox only**; `docs/runbooks/start-the-worker.md` and `docs/adr/0014-sandbox-mode.md`'s Context were corrected accordingly | Accepted |
| 2026-08-05 | — | /post drafts get a button-based category/region/tags picker (no AI call); fixing a category on a PublishFailed draft auto-reopens it | Accepted |
| 2026-08-06 | — | **Publish target is per draft, not per process:** the review card gains 🌐 Само сайт and 📘 Само ФБ next to ✅ Одобри, writing `nw_Draft.PublishTarget` (`Both` \| `Website` \| `Facebook`, migration 0016). `PublishJob` asks `PublishTargets.RequiredDestinations` per draft instead of computing one process-wide array at construction, and the two Facebook queries (link post after the site publish vs. standalone post) now both run each cycle, filtered by target, instead of being selected by `Publishing:FacebookOnly`. That flag survives as an ops kill-switch and **overrides the column for `Both` drafts only**: while it is on, the standalone query also accepts Both — otherwise a draft approved as Both, or scheduled with 📅 (which always writes Both), would wait forever for a site publish the flag has disabled. Website stays excluded even under the flag (🌐 is the editor's explicit "not Facebook"); a Website draft just waits until the flag clears. Each target button is gated on its destination being reachable: 🌐 hidden under the flag, 📘 hidden when Facebook is unconfigured — without that second gate a Facebook-only draft in a Facebook-less deployment is selected by no leg and sits `Approved` silently forever | Accepted |
